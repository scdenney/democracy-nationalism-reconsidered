# LaTeX build artifacts (kept local; Overleaf compiles its own)
*.aux
*.bbl
*.blg
*.bcf
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.synctex(busy)
*.toc
*.lof
*.lot

# Build output directories (Makefile -outdir) and any stray PDFs
out/
out-anon/
*.pdf

# Anonymization sentinel (created/removed by `make anon`)
.anon

# OS / editor cruft
.DS_Store
*~
*.swp
*.swo
